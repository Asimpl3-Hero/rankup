# Port exposed by docker-compose for frontend container.
RANKUP_PORT=8080

# Optional for non-docker runs if you want absolute API URL.
# By default, frontend uses relative /api and nginx proxy in Docker.
# VITE_RANKUP_API_URL=http://localhost:3000
